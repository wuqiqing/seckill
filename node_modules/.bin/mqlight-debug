#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../mqlight/bin/mqlight-debug.js" "$@"
  ret=$?
else 
  node  "$basedir/../mqlight/bin/mqlight-debug.js" "$@"
  ret=$?
fi
exit $ret
